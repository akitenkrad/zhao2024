[project]
name = "zhao2024-workspace"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "competeai-tools",
]

[tool.uv.workspace]
members = ["tools"]

[tool.uv.sources]
competeai-tools = { workspace = true }
